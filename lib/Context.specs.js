const SUT = require('./Context');
const expect = require('expect');
const describe = require('mocha').describe;
const it = require('mocha').it;

describe('Context', () => {
    describe('.created', () => {
        it('should change the status of the context to `meta`', () => {
            let sut = new SUT();
            
        })
    });

});